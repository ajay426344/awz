# group_vars/all.yml - Complete variables file
---
# Database configuration
# Database configuration - use existing AWX database
db_host: "awx-postgres-15.awx.svc.cluster.local"
db_name: awx  # Use existing awx database
db_schema: patch_tracking  # Our schema within awx database
db_user: awx
db_password: "{{ lookup('k8s', api_version='v1', kind='Secret', namespace='awx', resource_name='awx-postgres-configuration') | json_query('data.password') | b64decode }}"
# ESXi credentials
esxi_password: "Ajay@426344"

# Patch repository
patch_repository: "/mnt/patches"

# Dynatrace configuration
dynatrace_url: "https://ofp39563.live.dynatrace.com"
dynatrace_token: "{{ dynatrace_token }}"

# Configurable thresholds
default_threshold_minutes: 10
grace_period_seconds: 120
max_wait_seconds: 900
retry_count: 3

# Auto cleanup setting
auto_cleanup: true
